<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xwz.mapper.GrabDataMapper">
    <insert id="Crawling">
        INSERT INTO pic(title,filename,href,createtime) VALUES(#{title},#{filename},#{href},now())
    </insert>
</mapper>